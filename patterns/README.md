# Patterns

Biblioteca oficial de patrones de fraude para Estafometro.

- La fuente de verdad vive en estos YAML.
- `patterns:validate` valida el schema local.
- `patterns:sync` sincroniza el contenido a Supabase usando el endpoint interno o acceso directo desde script.
