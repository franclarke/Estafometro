/**
 * Re-exports from the auto-generated Supabase types for convenient access.
 *
 * Usage:
 *   import type { Tables, TablesInsert, Enums } from "@/server/db/supabase.types";
 *
 * Regenerate with:
 *   npx supabase gen types typescript --project-id <id> --schema public | Out-File -Encoding utf8 src/server/db/generated.types.ts
 */

export type {
  Database,
  Tables,
  TablesInsert,
  TablesUpdate,
  Enums,
} from "@/server/db/generated.types";
