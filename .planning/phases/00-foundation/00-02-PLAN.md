---
phase: 00-foundation
plan: 02
type: execute
wave: 1
depends_on: [00-01]
files_modified: []
autonomous: true
requirements: [RF-01, RF-18]

must_haves:
  truths:
    - "Project follows consistent folder structure by domain"
    - "ESLint and Prettier configured with sensible defaults"
    - "README.md explains how to run locally"
  artifacts:
    - path: "README.md"
      provides: "Local development guide"
      min_lines: 30
    - path: ".eslintrc.json"
      provides: "ESLint configuration"
    - path: "src/server/cases/index.ts"
      provides: "Server module entry point stub"
    - path: "src/components/case-intake/index.ts"
      provides: "Component module entry point stub"
  key_links:
    - from: "src/server/cases"
      to: "src/lib/supabase/server.ts"
      via: "imports createServerSupabaseClient"
    - from: "src/server/cases"
      to: "src/types/cases.ts"
      via: "imports Case, CaseEvidence types"
---

<objective>
Establish folder structure by domain, configure linting/formatting, and write README with local development guide. Follows the structure defined in plan_implementacion.md.
</objective>

<execution_context>
@C:/Users/franc/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@docs/plan_implementacion.md
@.planning/phases/00-foundation/00-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create folder structure by domain</name>
  <files>src/server/cases/index.ts, src/server/cases/types.ts, src/server/evidence/index.ts, src/server/preprocessing/index.ts, src/server/extraction/index.ts, src/server/entities/index.ts, src/server/signals/index.ts, src/server/risk/index.ts, src/server/patterns/index.ts, src/server/candidate-patterns/index.ts, src/server/enrichment/index.ts, src/server/explanations/index.ts, src/server/analytics/index.ts, src/server/privacy/index.ts, src/server/storage/index.ts, src/server/db/index.ts, src/components/case-intake/index.ts, src/components/evidence-upload/index.ts, src/components/analysis-progress/index.ts, src/components/result-summary/index.ts, src/components/risk-display/index.ts, src/components/signal-list/index.ts, src/components/external-findings/index.ts, src/components/recommendation-panel/index.ts, src/components/feedback-form/index.ts, src/app/api/cases/route.ts, src/app/api/cases/[caseId]/evidence/route.ts, src/app/api/cases/[caseId]/analyze/route.ts, src/app/api/cases/[caseId]/result/route.ts, src/app/api/cases/[caseId]/feedback/route.ts</files>
  <action>
    Create the directory structure as defined in plan_implementacion.md section 17.

    For each module, create an index.ts stub that exports an empty object or basic function, and a types.ts if the module needs types:

    **Server modules (src/server/):**
    - cases/ — index.ts + types.ts
    - evidence/ — index.ts
    - preprocessing/ — index.ts
    - extraction/ — index.ts
    - entities/ — index.ts
    - signals/ — index.ts
    - risk/ — index.ts
    - patterns/ — index.ts
    - candidate-patterns/ — index.ts
    - enrichment/ — index.ts
    - explanations/ — index.ts
    - analytics/ — index.ts
    - privacy/ — index.ts
    - storage/ — index.ts
    - db/ — index.ts

    **UI components (src/components/):**
    - case-intake/
    - evidence-upload/
    - analysis-progress/
    - result-summary/
    - risk-display/
    - signal-list/
    - external-findings/
    - recommendation-panel/
    - feedback-form/

    **API routes (src/app/api/):**
    - cases/route.ts (POST handler returning 501)
    - cases/[caseId]/evidence/route.ts
    - cases/[caseId]/analyze/route.ts
    - cases/[caseId]/result/route.ts
    - cases/[caseId]/feedback/route.ts

    **Pages (src/app/):**
    - page.tsx (Landing — basic shell)
    - caso/nuevo/page.tsx (Case creation)
    - caso/[publicId]/resultado/page.tsx (Result display)

    Each index.ts should have a comment like `// TODO: implement in Phase XX`
  </action>
  <verify>
    <automated>Get-ChildItem "src/server" -Recurse -Name | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>All module directories exist with index.ts stubs, API routes return 501 Not Implemented</done>
</task>

<task type="auto">
  <name>Task 2: Configure ESLint and Prettier</name>
  <files>.eslintrc.json, .prettierrc, .eslintignore</files>
  <action>
    **.eslintrc.json** — Next.js 15 + TypeScript strict:
    ```json
    {
      "extends": ["next/core-web-vitals", "next/typescript"],
      "rules": {
        "@typescript-eslint/no-unused-vars": ["error", { "argsIgnorePattern": "^_" }],
        "no-console": ["warn", { "allow": ["warn", "error"] }]
      }
    }
    ```

    **.prettierrc:**
    ```json
    {
      "semi": false,
      "singleQuote": true,
      "tabWidth": 2,
      "trailingComma": "es5",
      "printWidth": 100,
      "arrowParens": "avoid"
    }
    ```

    **.eslintignore:**
    ```
    node_modules/
    .next/
    .venv/
    *.config.js
    ```

    Add to package.json scripts:
    ```json
    "lint": "next lint",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
    ```

    Install dev dependencies:
    - prettier
    - eslint-config-prettier
  </action>
  <verify>
    <automated>Test-Path ".eslintrc.json" -and (Test-Path ".prettierrc")</automated>
  </verify>
  <done>ESLint and Prettier configured, scripts added to package.json</done>
</task>

<task type="auto">
  <name>Task 3: Write README with local development guide</name>
  <files>README.md</files>
  <action>
    Create README.md with:

    1. **Project description** (2-3 sentences from especificacion_funcional.md)
    2. **Tech stack** bullet list
    3. **Quick start** (5 steps: clone, install, env vars, db push, dev)
    4. **Architecture overview** — 2-3 sentences on the 8-module pipeline
    5. **Folder structure** — tree view of src/server/ and src/components/
    6. **Environment variables** — table of all vars from .env.example with description
    7. **Key commands** — npm scripts, supabase commands, vercel commands
    8. **Phase roadmap** — link to .planning/ROADMAP.md

    Keep it concise. This is a developer README, not a user-facing doc.
  </action>
  <verify>
    <automated>(Get-Content "README.md" | Measure-Object -Line).Lines -ge 30</automated>
  </verify>
  <done>README.md exists with quick start, architecture overview, folder structure, env vars table</done>
</task>

</tasks>

<verification>
- All server module directories have index.ts stubs
- All component directories have index.ts stubs
- ESLint config extends next/core-web-vitals
- Prettier config is in place
- README.md has all sections
</verification>

<success_criteria>
- Folder structure matches plan_implementacion.md section 17
- ESLint runs without errors on the scaffolded code
- README has quick start working
- Next.js dev server starts and shows landing page
</success_criteria>

<output>
After completion, create `.planning/phases/00-foundation/00-02-SUMMARY.md`
</output>
