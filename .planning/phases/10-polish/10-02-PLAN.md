---
phase: 10-polish
plan: 02
type: execute
wave: 5
depends_on: [10-01]
files_modified: []
autonomous: true
requirements: [RF-01, RF-02, RF-03, RF-04, RF-05, RF-06, RF-07, RF-10, RF-11, RF-12, RF-13, RF-14, RF-15, RF-16, RF-17, RF-18]

must_haves:
  truths:
    - "Critical path test suite passes"
    - "Full case flow can be executed in local environment"
    - "Example payloads demonstrate real use cases"
  artifacts:
    - path: "tests/case.test.ts"
      provides: "Case creation and evidence flow tests"
    - path: "tests/signals.test.ts"
      provides: "Signal detection and hard rules tests"
    - path: "tests/risk.test.ts"
      provides: "Risk scoring and band mapping tests"
    - path: "examples/cases/"
      provides: "Example case JSONs for testing"
  key_links:
    - from: "tests/signals.test.ts"
      to: "src/server/signals/index.ts"
      via: "tests signal detection"
    - from: "tests/risk.test.ts"
      to: "src/server/risk/index.ts"
      via: "tests risk scoring"
---

<objective>
Add minimal test coverage for critical path modules and create example case payloads. Ensure the system can be validated end-to-end without manual testing.
</objective>

<execution_context>
@C:/Users/franc/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/10-polish/10-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Set up test infrastructure</name>
  <files>tests/setup.ts, vitest.config.ts, package.json</files>
  <action>
    Add test dependencies to package.json:
    - vitest (or jest)
    - @testing-library/react
    - @testing-library/dom
    - @testing-library/jest-dom (for React component tests)

    **vitest.config.ts:**
    ```typescript
    import { defineConfig } from 'vitest/config'
    export default defineConfig({
      test: {
        environment: 'node',
        globals: true,
        setupFiles: ['tests/setup.ts'],
        include: ['tests/**/*.test.ts']
      }
    })
    ```

    **tests/setup.ts:**
    - Mock Supabase client for tests
    - Mock Gemini API calls
    - Set up test environment variables

    Add to package.json scripts:
    ```json
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
    ```
  </action>
  <verify>
    <automated>Test-Path "tests/setup.ts"</automated>
  </verify>
  <done>Test infrastructure with Vitest is configured</done>
</task>

<task type="auto">
  <name>Task 2: Write critical path tests</name>
  <files>tests/case.test.ts, tests/signals.test.ts, tests/risk.test.ts, tests/patterns.test.ts</files>
  <action>
    **tests/case.test.ts:**
    - createCase creates case with received status
    - createCase generates unique public_id
    - attachEvidence stores evidence for case
    - Case status transitions correctly

    **tests/signals.test.ts:**
    - detectSignals finds urgent_transfer in text
    - detectSignals finds asks_for_otp in text
    - Signals are deduplicated (same code, keep highest weight)
    - Signal weights are applied from catalog

    **tests/risk.test.ts:**
    - computeRiskScore returns 0-100
    - deriveRiskLevel maps 0-24 to 'low'
    - deriveRiskLevel maps 25-49 to 'medium'
    - deriveRiskLevel maps 50-74 to 'high'
    - deriveRiskLevel maps 75-100 to 'very_high'
    - applyHardRules floors 'high' when asks_for_otp present
    - applyHardRules floors 'very_high' when authority_impersonation + bribery_request

    **tests/patterns.test.ts:**
    - loadPatterns returns all official patterns
    - matchOfficialPatterns scores case with core signals
    - Fingerprint hashing is stable
    - Same signature produces same fingerprint

    Use mocked Supabase and Gemini. Tests should be fast (<100ms each).
  </action>
  <verify>
    <automated>npm test 2>&1 | Select-String "passed"</automated>
  </verify>
  <done>All critical path tests pass</done>
</task>

<task type="auto">
  <name>Task 3: Create example case payloads</name>
  <files>examples/cases/family_money_scam.json, examples/cases/online_purchase_fraud.json, examples/cases/bank_support_scam.json, examples/cases/authority_impersonation.json</files>
  <action>
    Create examples/cases/ directory with 4 anonymized real-world example cases:

    **examples/cases/family_money_scam.json:**
    ```json
    {
      "description": "Mensaje de 'hijo' con número nuevo pidiendo transferencia urgente",
      "narrative": "Hola mama, cambie de numero. Te escribo x un tema urgente, necesito que me transfieras 5000 pesos a mi alias MUEBLESXYZ porque tube un problema. No me da tiempo a explicarte, despues te cuento. Te lo paso por favor es para hoy.",
      "case_type": "family_money",
      "expected_signals": ["new_number_claim", "transfer_request", "emotional_pressure", "urgent_transfer"],
      "expected_risk_level": "high"
    }
    ```

    **examples/cases/online_purchase_fraud.json:**
    ```json
    {
      "description": "Vendedor de Mercado Libre desvía a WhatsApp y pide transferencia a alias",
      "narrative": "Hola! Vi tu публикации about the sofa. The seller says I can get a 40% discount if I transfer directly to their alias instead of through the platform. They say it's because they're moving and need quick cash. The listing was on Mercado Libre but they want me to pay via WhatsApp to their alias MUEBLESXYZ.",
      "case_type": "online_purchase",
      "expected_signals": ["platform_bypass", "off_platform_payment", "transfer_request"],
      "expected_risk_level": "high"
    }
    ```

    **examples/cases/bank_support_scam.json:**
    ```json
    {
      "description": "SMS de 'banco' pidiendo código de verificación",
      "narrative": "Your account has been locked. To unlock, verify your identity by providing the code we just sent you. Do not share this code with anyone. If you did not request this, ignore this message. Bank security team.",
      "case_type": "bank_support",
      "expected_signals": ["asks_for_otp", "authority_impersonation", "suspicious_link"],
      "expected_risk_level": "very_high"
    }
    ```

    **examples/cases/authority_impersonation.json:**
    ```json
    {
      "description": "Llamada de 'policía' amenazando con causa por un supuesto欠缴税款",
      "narrative": "This is Officer González from the judicial police. You have an outstanding warrant for your arrest due to unpaid taxes. To resolve this before we proceed with the arrest order, you need to make a payment of 10000 pesos to avoid the arrest. This is urgent, you have 2 hours. Do not contact anyone else about this matter.",
      "case_type": "authority_impersonation",
      "expected_signals": ["authority_impersonation", "bribery_request", "threatens_arrest", "urgent_transfer"],
      "expected_risk_level": "very_high"
    }
    ```

    These examples should be used for manual testing and as regression references.
  </action>
  <verify>
    <automated>(Get-ChildItem "examples/cases" -Filter "*.json" | Measure-Object).Count -eq 4</automated>
  </verify>
  <done>4 example case payloads exist for testing and reference</done>
</task>

</tasks>

<verification>
- Test suite runs with `npm test`
- All critical path tests pass
- Example cases can be submitted through the system
</verification>

<success_criteria>
- `npm test` passes without errors
- Hard rules tests correctly floor risk bands
- Pattern matching tests find matches for cases with core signals
- Example payloads cover the 4 main scam types
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish/10-02-SUMMARY.md`
</output>
