---
phase: 10-polish
plan: 01
type: execute
wave: 5
depends_on: [09-01]
files_modified: []
autonomous: true
requirements: [RF-01, RF-02, RF-03, RF-04, RF-05, RF-06, RF-07, RF-10, RF-11, RF-12, RF-13, RF-14, RF-15, RF-16, RF-17, RF-18]

must_haves:
  truths:
    - "README explains how to run locally and deploy"
    - "Architecture document shows 8-module pipeline"
    - "Migration guide documents how to add new patterns"
  artifacts:
    - path: "README.md"
      provides: "Complete developer guide"
      min_lines: 80
    - path: "docs/architecture.md"
      provides: "Architecture overview"
      min_lines: 60
    - path: "docs/migrations.md"
      provides: "Migration guide"
    - path: "docs/patterns.md"
      provides: "Pattern authoring guide"
  key_links:
    - from: "docs/architecture.md"
      to: "src/server/preprocessing/index.ts"
      via: "documents pipeline modules"
---

<objective>
Create complete technical documentation: README, architecture overview, migration guide, and pattern authoring guide. Ensure another developer or agent can run the project based solely on these docs.
</objective>

<execution_context>
@C:/Users/franc/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/09-privacy/09-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README with complete documentation</name>
  <files>README.md</files>
  <action>
    Expand README.md to include:

    1. **Project description** — 2 paragraphs on what Estafometro does and who it's for
    2. **Quick start** — 5 steps to get running locally
    3. **Tech stack** — bullet list with version requirements
    4. **Architecture overview** — text diagram of the 8-module pipeline:
       intake → preprocessing → extraction → signals → enrichment → patterns → risk → explanation
    5. **Folder structure** — tree with descriptions of key directories
    6. **Environment variables** — table with all vars from .env.example
    7. **Key commands** — npm run dev, supabase db push, vercel deploy, etc.
    8. **API reference** — brief table of endpoints with input/output
    9. **Phase roadmap** — link to .planning/ROADMAP.md
    10. **Contributing** — how to add patterns, signals, or checkers
  </action>
  <verify>
    <automated>(Get-Content "README.md" | Measure-Object -Line).Lines -gt 80</automated>
  </verify>
  <done>README is comprehensive with 80+ lines</done>
</task>

<task type="auto">
  <name>Task 2: Create architecture documentation</name>
  <files>docs/architecture.md</files>
  <action>
    Create docs/architecture.md with:

    1. **System overview** — high-level description of the pipeline
    2. **8-module pipeline** — each module with:
       - Purpose
       - Inputs
       - Outputs
       - Key functions
    3. **Data flow** — how a case moves through the pipeline
    4. **Decision architecture** — how scoring works (signals + hard rules, NOT LLM)
    5. **Key design decisions** — bullets from especificacion_tecnica.md section 18
    6. **Database schema** — ERD-style text diagram
    7. **Security model** — RLS, rate limiting, API key management
    8. **Privacy model** — retention policies, what we store vs. delete
  </action>
  <verify>
    <automated>Test-Path "docs/architecture.md"</automated>
  </verify>
  <done>Architecture doc exists with pipeline flow and design decisions</done>
</task>

<task type="auto">
  <name>Task 3: Create migration and pattern authoring guides</name>
  <files>docs/migrations.md, docs/patterns.md</files>
  <action>
    **docs/migrations.md:**
    - How to create a new migration: `supabase migration new migration_name`
    - How to run migrations locally: `supabase db push`
    - How to reset local DB: `supabase db reset`
    - How to inspect production DB
    - Naming conventions for migrations
    - How to add a new table
    - How to add columns to existing tables

    **docs/patterns.md:**
    - How to add a new pattern (create patterns/{category}/pattern.json)
    - Pattern JSON schema with field descriptions
    - How to sync patterns to DB: POST /api/internal/patterns/sync
    - How to promote a candidate pattern to official
    - Version management
    - Testing a new pattern locally
  </action>
  <verify>
    <automated>(Test-Path "docs/migrations.md") -and (Test-Path "docs/patterns.md")</automated>
  </verify>
  <done>Migration guide and pattern authoring guide exist</done>
</task>

</tasks>

<verification>
- README has complete quick start
- Architecture doc covers all 8 modules
- Migration guide documents all key operations
- Pattern guide enables non-technical contribution
</verification>

<success_criteria>
- `npm run dev` works after following README
- `supabase db push` works after following migration guide
- Pattern can be added following pattern guide
- Architecture doc explains why LLM is not the scorer
</success_criteria>

<output>
After completion, create `.planning/phases/10-polish/10-01-SUMMARY.md`
</output>
