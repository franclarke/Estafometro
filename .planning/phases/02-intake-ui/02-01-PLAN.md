---
phase: 02-intake-ui
plan: 01
type: execute
wave: 2
depends_on: [01-02]
files_modified: []
autonomous: true
requirements: [RF-01, RF-02, RF-03, RF-04, RF-11, RF-12]

must_haves:
  truths:
    - "User can create a case and see confirmation without login"
    - "User can write narrative text and attach evidence"
    - "Evidence uploads are validated for size and type"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Landing page with CTA"
      min_lines: 30
    - path: "src/app/caso/nuevo/page.tsx"
      provides: "Case creation page"
      min_lines: 50
    - path: "src/components/case-intake/CaseForm.tsx"
      provides: "Case creation form component"
    - path: "src/components/evidence-upload/EvidenceUploader.tsx"
      provides: "Evidence upload component"
  key_links:
    - from: "src/app/caso/nuevo/page.tsx"
      to: "src/components/case-intake/CaseForm.tsx"
      via: "renders CaseForm"
    - from: "src/components/case-intake/CaseForm.tsx"
      to: "src/lib/db/repositories.ts"
      via: "calls createCase on submit"
    - from: "src/components/evidence-upload/EvidenceUploader.tsx"
      to: "src/lib/supabase/client.ts"
      via: "uploads to Supabase Storage"
---

<objective>
Build the case intake UX — landing page, case creation form with narrative input, evidence upload (screenshots, URLs, usernames), and client-side state management. No authentication required.
</objective>

<execution_context>
@C:/Users/franc/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@docs/especificacion_funcional.md
@docs/especificacion_tecnica.md
@.planning/phases/01-data-model/01-02-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build landing page</name>
  <files>src/app/page.tsx, src/components/landing/HeroSection.tsx, src/components/landing/HowItWorks.tsx, src/components/landing/TrustSignals.tsx</files>
  <action>
    Create the landing page at src/app/page.tsx with:

    **HeroSection** — prominent headline in Spanish:
    "Evaluá si es una estafa antes de transferir o compartir datos"
    Subtext: "Gratuito, sin registro, anónimo. Tu caso se analiza en segundos."
    CTA button: "Empezar evaluación" → links to /caso/nuevo

    **HowItWorks** — 3-step visual:
    1. Contás tu caso (narrativa libre)
    2. Agregás evidencia (opcional)
    3. Recibís una evaluación con señales y recomendación

    **TrustSignals** — brief trust builders:
    - "No guardamos tus mensajes ni capturas"
    - "No necesitás crear cuenta"
    - "Evaluación orientativa, no certificación"

    Use Tailwind classes. Follow a clean, editorial style — NOT generic AI aesthetics. Use the project's design language. The app should feel serious and trustworthy, like a financial tool.
  </action>
  <verify>
    <automated>Get-Content "src/app/page.tsx" | Select-String "Empezar evaluación" | Measure-Object | Select-Object -ExpandProperty Count</automated>
  </verify>
  <done>Landing page exists with hero, how it works, and trust signals sections</done>
</task>

<task type="auto">
  <name>Task 2: Build case creation form with narrative input</name>
  <files>src/app/caso/nuevo/page.tsx, src/components/case-intake/CaseForm.tsx, src/components/case-intake/NarrativeInput.tsx, src/components/case-intake/OptionalFields.tsx</files>
  <action>
    Create the case creation page at src/app/caso/nuevo/page.tsx.

    **CaseForm.tsx** — main form component with:
    - **NarrativeInput** — large textarea (min 200px height) with placeholder: "Describí qué pasó. Por ejemplo: 'Me escribió un número desconocido por WhatsApp diciendo que era mi hijo y que tenía un problema urgente...'"
    - **OptionalFields** — collapsible section with:
      - URL input
      - Username/alias input
      - Phone number input
    - **EvidenceUploader** — drag-and-drop zone for screenshots (see Task 3)
    - Submit button: "Analizar mi caso"
    - Client-side validation: narrative must be at least 20 characters

    **Navigation:**
    After successful case creation, navigate to /caso/[publicId]/resultado with the case in loading state (the user sees the "analizando" screen while the backend processes).

    **State management:**
    Use React useState/useReducer to manage form state locally. Store case_public_id in component state (not URL params initially) until the case is created and user is redirected.

    **API integration:**
    On form submit, call POST /api/cases with { narrative_text }.
    If evidence files exist, after case creation call POST /api/cases/:id/evidence for each file.
  </action>
  <verify>
    <automated>Test-Path "src/components/case-intake/CaseForm.tsx"</automated>
  </verify>
  <done>CaseForm component exists with narrative input, optional fields, and submit handler</done>
</task>

<task type="auto">
  <name>Task 3: Build evidence upload component</name>
  <files>src/components/evidence-upload/EvidenceUploader.tsx, src/components/evidence-upload/PreviewGallery.tsx, src/lib/storage.ts</files>
  <action>
    **EvidenceUploader.tsx:**
    - Drag-and-drop zone with visual feedback
    - Click to browse fallback
    - Accept: image/png, image/jpeg, image/webp (max 10MB per file, max 5 files)
    - Show upload progress indicator
    - Preview thumbnails for selected images
    - Remove button on each file
    - Store files in Supabase Storage under cases/{caseId}/evidence/{evidenceId}

    **PreviewGallery.tsx:**
    - Grid display of uploaded evidence thumbnails
    - Click to enlarge in modal
    - Show file name and size

    **src/lib/storage.ts:**
    ```typescript
    export async function uploadEvidence(caseId: string, file: File): Promise<string> {
      // Upload to Supabase Storage, return storage path
    }

    export function getPublicStorageUrl(storagePath: string): string {
      // Generate public URL for storage path
    }
    ```

    Validation:
    - File size max 10MB
    - Total upload max 50MB
    - Only allow image types
  </action>
  <verify>
    <automated>Test-Path "src/components/evidence-upload/EvidenceUploader.tsx"</automated>
  </verify>
  <done>EvidenceUploader supports drag-drop, preview, and upload to Supabase Storage</done>
</task>

<task type="auto">
  <name>Task 4: Create analysis-in-progress screen</name>
  <files>src/app/caso/[publicId]/analizando/page.tsx, src/components/analysis-progress/AnalysisProgress.tsx</files>
  <action>
    Create the analysis-in-progress page at src/app/caso/[publicId]/analizando/page.tsx.

    **AnalysisProgress.tsx:**
    - Animated loading indicator (CSS spinner or pulse animation, NOT a generic spinner)
    - Status text: "Revisando tu caso..." with animated ellipsis
    - What to expect: brief list of 3-4 things happening (extracting entities, detecting signals, checking patterns)
    - Don't show a progress bar (we don't have real progress %)
    - After 30 seconds, show "Está tardando más de lo usual" message

    **Polling:**
    While on this screen, poll GET /api/cases/:caseId/result every 3 seconds.
    If result is available, redirect to /caso/[publicId]/resultado.
    If an error occurs, redirect to an error state with "Intentar de nuevo" option.

    This screen prevents the user from thinking the app is broken during analysis.
  </action>
  <verify>
    <automated>Test-Path "src/app/caso/[publicId]/analizando/page.tsx"</automated>
  </verify>
  <done>Analysis progress screen polls for result and redirects when ready</done>
</task>

</tasks>

<verification>
- Landing page renders with all sections
- Case creation form submits and navigates to progress screen
- Evidence upload validates file types and sizes
- Polling redirects to result when analysis completes
</verification>

<success_criteria>
- User can complete full intake flow from landing to analysis-in-progress screen
- Narrative input is required (20 char min)
- Evidence upload accepts images under 10MB
- Client-side validation prevents empty submissions
</success_criteria>

<output>
After completion, create `.planning/phases/02-intake-ui/02-01-SUMMARY.md`
</output>
